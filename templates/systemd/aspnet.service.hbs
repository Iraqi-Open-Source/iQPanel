[Unit]
Description=ASP.NET Core app - {{slug}}
After=network.target

[Service]
User={{run_as_user}}
WorkingDirectory={{site_path}}/app/out
ExecStart=/usr/bin/dotnet {{site_path}}/app/out/MyApp.dll --urls=http://127.0.0.1:{{port}}
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
