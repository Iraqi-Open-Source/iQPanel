[Unit]
Description=Celery worker - {{slug}}
After=network.target

[Service]
User={{run_as_user}}
WorkingDirectory={{site_path}}/app
ExecStart={{site_path}}/venv/bin/celery -A {{python_module}} worker --loglevel=INFO
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
