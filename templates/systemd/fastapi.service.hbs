[Unit]
Description=FastAPI app - {{slug}}
After=network.target

[Service]
User={{run_as_user}}
WorkingDirectory={{site_path}}/app
ExecStart={{site_path}}/venv/bin/uvicorn app.main:app --host 127.0.0.1 --port {{port}}
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
