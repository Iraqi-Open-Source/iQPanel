[Unit]
Description=Gunicorn application - {{slug}}
After=network.target

[Service]
User={{run_as_user}}
WorkingDirectory={{site_path}}/app
ExecStart={{site_path}}/venv/bin/gunicorn --bind 127.0.0.1:{{port}} {{python_module}}
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
